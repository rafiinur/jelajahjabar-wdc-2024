/* eslint-disable react/prop-types */
const SectionDescription = ({ children, className }) => {
  return <p className={`text-lg font-light ${className}`}>{children}</p>
}

export default SectionDescription
